# Contributing

Keep changes small and tied to the live SlurpNet Icarus server contract.

Before opening a PR:

```bash
scripts/validate-icarus-release.sh
```

Never include secrets or generated pak files in commits.

